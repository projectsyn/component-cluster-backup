parameters:
  cluster_backup:
    namespace: syn-cluster-backup
    keepjobs: 5
    bucket:
      name: k8s-objects
    images:
      object_dumper:
        image: docker.io/projectsyn/k8s-object-dumper
        tag: v0.1.0
