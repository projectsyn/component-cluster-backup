parameters:
  kapitan:
    compile:
      - input_paths:
          - cluster-backup/component/app.jsonnet
        input_type: jsonnet
        output_path: .
      - input_paths:
          - cluster-backup/component/main.jsonnet
        input_type: jsonnet
        output_path: cluster-backup/
